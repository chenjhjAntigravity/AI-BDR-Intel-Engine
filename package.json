{
  "name": "bdr-intel-worker",
  "version": "1.0.0",
  "description": "Cloudflare BDR OSINT Worker with MailChannels",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.1.0",
    "postal-mime": "^2.7.4"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
